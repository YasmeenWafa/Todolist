This simple todolist app is build using React Hooks.

### How to run:
1. clone this repo
2. cd to Todolist folder
3. install node-modules by running the command: npm install
4. start the app by running the command: npm start
